spring:
  datasource:
    url: ${HEROKU_POSTGRESQL_AQUA_URL}
  jpa:
    hibernate:
      ddl-auto: none
      format_sql: false
    show-sql: false
    open-in-view: false

  flyway:
    enabled: true

service:
  ranking:
    editors-choice: 29